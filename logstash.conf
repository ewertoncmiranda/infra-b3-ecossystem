input {
  tcp {
    port => 5000
    codec => json_lines
  }

  udp {
    port => 5000
    codec => json_lines
  }
}

filter {
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "b3-logs-%{+YYYY.MM.dd}"
  }

  stdout {
    codec => rubydebug
  }
}
